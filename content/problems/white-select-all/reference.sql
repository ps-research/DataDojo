-- Recipe 1.1 tutorial: return the whole table.
-- "*" is shorthand for "every column"; with no WHERE clause, every row of EMP
-- is returned. This is the simplest possible query and the natural starting
-- point for exploring an unfamiliar table.
SELECT *
FROM emp;
