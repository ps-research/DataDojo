# Copyrighted source material — never commit (pipeline reads it from disk)
Resources/
# KB artifact holds verbatim bronze spans of the book — local build artifact,
# regenerated by kb/pipeline; never commit
kb/datadojo_kb.sqlite
kb/reports/toc_raw.txt

# Local engine data dirs (verification farm)
kb/.pgdata/
kb/.mariadata/

# Dependencies & builds
node_modules/
dist/
build/
*.pyc
__pycache__/

# Env & secrets
.env
.env.*
!.env.example

# Scratch
*.log
.DS_Store

# local-only, never commit
VULTR_SSH_KEY.txt
app/.mongodata/
app/api/node_modules/
.ipynb_checkpoints/
